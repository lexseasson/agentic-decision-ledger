name: decision-admissibility

on:
  pull_request:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Agentic Decision Ledger gate (autodetect contracts)
        uses: ./integrations/github-action
        with:
          strict: "true"
          artifacts-dir: artifacts
          maintenance-contract: "decisions/contracts/DC-REPO-001.yaml"
          install-demo-contract: "decisions/contracts/DC-INSTALL-DEMO-001.yaml"
          example-contract: "decisions/contracts/DC-2026-001.yaml"
